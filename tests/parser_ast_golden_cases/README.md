# Parser AST Golden Cases

This corpus verifies parser semantic shape by comparing normalized AST dumps (`.ast`) against checked-in golden files.

Rules:
1. Keep `.pr` inputs minimal and focused.
2. Golden files are generated by `parus_parser_ast_golden_tests` with `PARUS_UPDATE_GOLDEN=1`.
3. Cases in this directory must parse without parser errors.
