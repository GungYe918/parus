macro add -> {
    with expr {
        (a: expr, b: expr) => expr {
            $0 + $1
        };
    }
}

def helper(x: i32) -> i32 {
    return $add(x, 3i32);
}

def main() -> i32 {
    return helper(x: 39i32);
}
