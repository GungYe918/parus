// OIR->LLVM 케이스: acts for 기반 operator(+) 오버로딩
acts for i32 {
    operator(+)(self a: i32, rhs: i32) -> i32 {
        return a;
    }
};

fn main() -> i32 {
    let a: i32 = 1i32;
    let b: i32 = 2i32;
    let c: i32 = a + b;
    return c;
}
