const def sum_to(n: i32) -> i32 {
    set mut i = 0i32;
    set mut acc = 0i32;
    while (i < n) {
        acc = acc + i;
        i = i + 1i32;
    }
    return acc;
}

const SUM10: i32 = sum_to(10i32);

def main() -> i32 {
    return SUM10;
}
