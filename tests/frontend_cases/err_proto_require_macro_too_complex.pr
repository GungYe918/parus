macro runknown -> {
    with expr {
        () => expr { unknown_flag };
    }
}

proto P {
    def f(self) -> i32;
} with require($runknown());

class A: P {
    init() = default;
    def f(self) -> i32 { return 1i32; }
};

def main() -> i32 {
    return 0i32;
}
