# Frontend Integration Cases

This directory contains regression cases that require passes/tyck/cap/sir/oir
validation beyond parser-only checks.

These files are executed by:

- `parus_frontend_integration_tests`

Parser-only syntax/recovery cases remain in `tests/parser_cases/`.
