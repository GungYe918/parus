fn main() -> i32 {
    let x: i32 = 10;
    let y: i32 = 20;

    // 기본 산술
    let z: i32 = x + y;

    // while ( ... ) { ... }
    let mut i: i32 = 0;
    while (i < 3) {
        i = i + 1;
    }

    return z;
}
