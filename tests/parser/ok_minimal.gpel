fn add(a: int) -> int {
  switch (1) {
    case 1: {}
    case 2: {}
    default : { return 42; }
  }

  return 42;
}

switch (tok) {
  case Foo: { return 1; }
}

switch (v) {
  case true: { return 1; }
  case false: { return 0; }
  case null: { return 2; }
}

switch (ch) {
  case 'A': { return 10; }
  case "hi": { return 20; }
}

// |> 연산자 테스트
a |> f(x: _, y: 1);
f(x: _, y: 1) <| a;
a |> f(_, 1);
a |> f({x: _, y: 1});

// 비트 쉬프트 연산 테스트
a >> 2;
b << 6;

fn add() -> int {
  
  // 이건 변수 대입
  set x = loop (x in xs) {
    break 42;
  }
  
  // 이건 그냥 제어문
  loop(x in xs) {
    break 42;
  }

  // loop 블럭 
  loop {
    break 42;
  }
}