extern "C" def c_add_i32(a: i32, b: i32) -> i32;
extern "C" def c_mul_i32(a: i32, b: i32) -> i32;

def main() -> i32 {
    let s: i32 = c_add_i32(a: 20i32, b: 22i32);
    let p: i32 = c_mul_i32(a: s, b: 2i32);
    if (p == 84i32) {
        return 0i32;
    }
    return 1i32;
}
