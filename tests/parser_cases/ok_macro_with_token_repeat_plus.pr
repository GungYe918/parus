macro sum_plus -> {
    with token {
        ($( $x: expr ),+) => stmt {
            {
                $( $x; )+
            }
        };
    }
}

def main() -> i32 {
    $sum_plus(4i32, 5i32);
    return 0i32;
}
