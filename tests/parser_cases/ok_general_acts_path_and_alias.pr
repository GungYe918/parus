acts Math {
    def add(a: i32, b: i32) -> i32 {
        return a + b;
    }
};

def main() -> i32 {
    let a: i32 = acts(Math)::add(1, 2);
    use acts(Math) as m;
    let b: i32 = m::add(3, 4);
    return a + b;
}
