nest world {
    def add(a: i32, b: i32) -> i32 {
        return a + b;
    }
}

def main() -> i32 {
    do {
        use nest world as w;
        set t = w::add(1, 2);
    };

    return w::add(3, 4);
}
