struct Vec2 {
    x: i32;
    y: i32;
};

acts for Vec2 {
    def add(self, b: i32) -> i32 {
        return self.x + b;
    }
};

def main() -> i32 {
    let v: Vec2 = Vec2 { x: 20, y: 0 };
    let r: i32 = Vec2::acts(default)::add(&v, 22);
    return r;
}
