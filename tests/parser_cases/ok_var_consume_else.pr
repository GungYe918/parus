def pop(opt: i32?) -> i32 {
  let mut local: i32? = opt;
  let v: i32 = local else {
    return 0i32;
  };
  return v;
}

def pop_index(xs: i32?[]) -> i32 {
  set mut arr = xs;
  set v = arr[0usize] else {
    return 1i32;
  };
  return v;
}
