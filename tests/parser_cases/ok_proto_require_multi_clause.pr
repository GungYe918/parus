proto P {
  def id(self) -> i32;
} with require(true), require(not false), require(true == true);

class A: P {
  id_value: i32;
  init() = default;

  def id(self) -> i32 {
    return 1i32;
  }
};

def main() -> i32 {
  set a = A();
  return a.id();
}
