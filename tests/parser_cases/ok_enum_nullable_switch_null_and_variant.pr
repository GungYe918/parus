enum Token {
    case Eof,
    case Int(value: i32),
}

def main() -> i32 {
    let t: Token? = null;
    switch (t) {
    case null: { return 0i32; }
    case Token::Int(value: v): { return v; }
    case Token::Eof: { return -1i32; }
    }
    return -2i32;
}
