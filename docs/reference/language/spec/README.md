# Split Spec Chapters

이 디렉터리는 기존 `docs/spec_v0.md`를 top-level section 기준으로 분해한 챕터 파일을 저장한다.

규칙:

1. 파일명은 순서 보존을 위해 `NN.md` 형식을 사용한다.
2. 의미 해석은 `docs/reference/language/SPEC.md`를 통해 접근한다.
3. EBNF 정본은 `28.md`를 따른다.
