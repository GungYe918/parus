plan bad_bundle = bundle & {
  name = "bad";
  kind = "lib";
  modules = [
    module & {
      head = "bad";
      sources = ["bad/src/lib.pr"];
      imports = [];
    },
  ];
  deps = [];
};

plan master = master & {
  project = { name: "bad", version: "0.1.0" };
  bundles = [bad_bundle];
  tasks = [];
  codegens = [];
};
