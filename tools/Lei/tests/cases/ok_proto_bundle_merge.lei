proto MyBundle {
  name: string;
  kind: string = "lib";
  deps: [string] = [];
};

export plan core_bundle = bundle & MyBundle & {
  name = "core";
  modules = [
    module & {
      sources = ["src/lib.pr"];
      imports = [];
    },
  ];
};

plan master = master & {
  project = {
    name: "demo",
    version: "0.1.0",
  };
  bundles = [core_bundle];
  tasks = [];
  codegens = [];
};
