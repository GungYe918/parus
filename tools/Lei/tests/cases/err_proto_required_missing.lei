proto BundleReq {
  name: string;
  kind: string = "lib";
  deps: [string] = [];
};

plan bad_bundle = bundle & BundleReq & {
  modules = [
    module & {
      head = "bad";
      sources = ["src/lib.pr"];
      imports = [];
    },
  ];
};

plan master = master & {
  project = {
    name: "proto-required-missing",
    version: "0.1.0",
  };
  bundles = [bad_bundle];
  tasks = [];
  codegens = [];
};
