plan a_bundle = bundle & {
  name = "a";
  kind = "lib";
  modules = [
    module & {
      sources = ["net/api/src/a.pr"];
      imports = [];
    },
  ];
  deps = [];
};

plan b_bundle = bundle & {
  name = "b";
  kind = "lib";
  modules = [
    module & {
      sources = ["net/process/src/b.pr"];
      imports = [];
    },
  ];
  deps = [];
};

plan master = master & {
  project = { name: "collision", version: "0.1.0" };
  bundles = [a_bundle, b_bundle];
  tasks = [];
  codegens = [];
};
