# parusc Docs

`parusc`는 사용자 진입점 드라이버다.

## 문서 인덱스

1. `CLI.md`: CLI 옵션과 모드
2. `DriverP0.md`: driver/p0 계층 구조
3. `Extensions.md`: 확장 포인트 상태
4. `LSP_MODE.md`: `parusc lsp --stdio` 위임 경로
5. `DIAGNOSTICS.md`: 진단 포맷/종료 코드

## 코드 근거

1. `compiler/parusc/src/cli/Options.cpp`
2. `compiler/parusc/src/driver/Driver.cpp`
3. `compiler/parusc/src/p0/P0Compiler.cpp`
